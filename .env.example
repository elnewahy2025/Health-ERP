# === DATABASE ===
DB_HOST=127.0.0.1
DB_PORT=5432
DB_NAME=healthcare
DB_USER=postgres
DB_PASSWORD=omar2009!

# === REDIS ===
REDIS_HOST=localhost
REDIS_PORT=6379
# Set true when Redis is required for this deployment’s workers/queues.
REDIS_REQUIRED=false
# Set true when external object storage is required instead of local filesystem storage.
OBJECT_STORAGE_REQUIRED=false
# Set true in production deployments where all durable workers must be running.
WORKERS_REQUIRED=false

# === AUTH ===
JWT_SECRET=change_this_in_production
JWT_REFRESH_SECRET=change_this_in_production

# === STORAGE ===
MINIO_ENDPOINT=localhost
MINIO_PORT=9000
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=your_minio_secret
MINIO_BUCKET=healthcare

# === EMAIL (SendGrid or SMTP) ===
SMTP_HOST=localhost
SMTP_PORT=587
SMTP_FROM=noreply@visionhealthcare.com
SENDGRID_API_KEY=your_sendgrid_key

# === SMS (Twilio) ===
TWILIO_ACCOUNT_SID=your_twilio_sid
TWILIO_AUTH_TOKEN=your_twilio_token
TWILIO_PHONE_NUMBER=+201234567890

# === WHATSAPP BUSINESS API ===
WHATSAPP_API_TOKEN=your_whatsapp_token
WHATSAPP_PHONE_NUMBER_ID=your_phone_number_id
WHATSAPP_BUSINESS_ACCOUNT_ID=your_business_account_id
WHATSAPP_WEBHOOK_VERIFY_TOKEN=vision-hc-whatsapp-verify

# === PAYMENTS ===
STRIPE_SECRET_KEY=sk_test_xxx
STRIPE_WEBHOOK_SECRET=whsec_xxx
FAWRY_MERCHANT_CODE=your_fawry_code
FAWRY_SECURITY_KEY=your_fawry_key
# === SUPABASE (File Storage) ===
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_KEY=your_service_key
SUPABASE_BUCKET=documents

# === SENTRY ERROR MONITORING ===
SENTRY_DSN=https://your_dsn@sentry.io/project_id
APP_VERSION=1.0.0
APP_COMMIT_SHA=

# === BACKUP ===
BACKUP_S3_BUCKET=vision-healthcare-backups
BACKUP_ENCRYPTION_KEY=your_encryption_key
BACKUP_RETENTION=7

# === APP ===
CORS_ORIGIN=https://yourdomain.com
APP_URL=https://yourdomain.com
NODE_ENV=development
PORT=3000
HOST=0.0.0.0